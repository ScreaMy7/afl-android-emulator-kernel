# Build outputs
out/
kernel-src/

# Prebuilt binaries -> published as GitHub Release assets, not committed
prebuilt/*.img
prebuilt/Image-*
prebuilt/modules/
prebuilt/*.tar
prebuilt/*.tar.*

# Logs
*.log

# Stray runtime artifacts (mitmproxy capture DB, etc.)
mitm_mcp_traffic.db
*.db

# macOS
.DS_Store
